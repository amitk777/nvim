return {
  "linux-cultist/venv-selector.nvim",
  dependencies = { "neovim/nvim-lspconfig", "nvim-telescope/telescope.nvim", "mfussenegger/nvim-dap-python" },
  opts = {
    -- Your options go here
    -- name = "venv",
    -- auto_refresh = false
  },
  --  event = "VeryLazy", -- Optional: needed only if you want to type `:VenvSelect` without a keymapping
  keys = {
    -- Keymap to open VenvSelector to pick a venv.
    { "<leader>pv", "<cmd>:VenvSelect<cr>",       desc = 'Py[ Virtual Env' },
    { "<leader>pc", "<cmd>:VenvSelectCached<cr>", desc = 'Py Virtual Env Cached' },
    -- Keymap to retrieve the venv from a cache (the one previously used for the same project directory).
    --   "<leader>vc", "<cmd>:VenvSelectCached<cr>"
  },
  --path = '/home/amit/anaconda3/envs',
  --name = { 'envs' },
  anaconda_base_path = "/home/amit/anaconda3",
  anaconda_envs_path = "/home/amit/anaconda3/envs",
}
